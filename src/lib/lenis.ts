'use client'

// Lenis removed — native browser scroll is smoother.
// Stub kept so existing imports don't break.
export const lenisRef: { current: null } = { current: null }

export function useLenis() {
  // no-op
}
